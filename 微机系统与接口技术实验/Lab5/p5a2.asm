DATAS SEGMENT
    ;此处输入数据段代码  
    DATA1 DB 41H,42H,43H,44H,45H,45H,47H,48H,49H,50H,58H
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    
    MOV CH,0
    MOV SI,OFFSET DATA1
AGAIN:
	MOV AL,[SI]
	CMP AL,0
	JE DONE
	MOV DL,AL
	MOV AH,02H	
	INT 21H
	MOV DL,' '
	MOV AH,02H	
	INT 21H
	INC CH
	TEST CH,01H
	JNZ NEXT
	MOV DL,0AH
	MOV AH,02H
	INT 21H
	
NEXT:
	INC SI
	
	JMP AGAIN
DONE:
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START

