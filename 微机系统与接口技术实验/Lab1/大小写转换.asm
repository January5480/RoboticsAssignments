DATAS SEGMENT
    ;此处输入数据段代码
 	str1 DB '1234!!ghijklnmopqrstuvwxyzABCDEFGHIGKLNMOPQRSTUVWXYZ$'	
DATAS ENDS
CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    MOV CX,52
    LEA SI,STR1
AGAIN:
	MOV AL,[SI];
	CMP AL,'A'
	JB NEXT
	CMP AL,'Z'
	JAE GREAT
	ADD AL,20H
	JMP NEXT
GREAT:
	CMP AL,'a'
	JB NEXT
	SUB AL,20H
NEXT:
	MOV [SI],AL
	INC SI
	DEC CX
	JNZ AGAIN
	LEA DX,STR1
	MOV AH,09H
	INT 21H
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
