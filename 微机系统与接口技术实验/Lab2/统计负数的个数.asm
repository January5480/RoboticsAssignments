DATAS SEGMENT
    ;此处输入数据段代码  
    DATA DB -1,3,-5,7,6,-4,9
    COUNT DW 0
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    MOV SI,OFFSET DATA
    MOV CX,7
    MOV BX,0
AGAIN:
	MOV AX,[SI]
	AND AX,AX
	JNS NEGC
	INC BX
NEGC:
	INC SI
	DEC CX
	JNZ AGAIN
	MOV COUNT,BX
	MOV DX,OFFSET COUNT
	MOV AH,9
	INT 21H
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
