DATAS SEGMENT
    ;此处输入数据段代码 
    STR1 DB 'ENTER:$'
    STR2 DB 'CORRECT!$'
    STR3 DB 'WRONG!ENTERAGEIN$' 
    BUF DB 6,0
    BUF1 DB '12344'
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
INPUTLOOP:
	MOV DX,OFFSET STR1
	MOV AH,9
	INT 21H
	MOV DX,OFFSET BUF
	MOV AH,0AH
	INT 21H
	MOV SI,OFFSET BUF
	MOV DI,OFFSET BUF1
	CLD
	MOV CX,5
	REPNZ CMPSW
	JZ CORRECT
	MOV DX,OFFSET STR3
	MOV AH,9
	INT 21H
	JMP INPUTLOOP
	MOV AH,4CH	
    INT 21H 
CORRECT:
	MOV DX,OFFSET STR2
	MOV AH,9
	INT 21H	
    MOV AH,4CH	
    INT 21H
CODES ENDS
    END START






