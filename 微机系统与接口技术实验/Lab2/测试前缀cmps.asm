DATAS SEGMENT
    ;此处输入数据段代码 
    DATA1 DB '1111$'
    DATA2 DB '12344$' 
    STR1 DB 'EQUAL!$'
    STR2 DB 'NOT EQUAL!$'
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    MOV SI,OFFSET DATA1
    MOV DI,OFFSET DATA2
    CLD
    MOV CX,4
    REPZ CMPSB
    JZ EQQ
    MOV DX,OFFSET STR2
	MOV AH,9
	INT 21H
	JMP DONE
EQQ:
	MOV DX,OFFSET STR1
	MOV AH,9
	INT 21H
DONE:
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START


